Псевдокод линейного поиска:

for i = 1 to A.length
	if A[i] == x return i
return NULL

Инвариантом здесь является тот факт, что в любой момент времени либо будет известен
индекс нужного нам элемента, либо он будет неизвестен.